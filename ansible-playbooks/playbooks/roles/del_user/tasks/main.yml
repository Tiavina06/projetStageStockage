#SPDX-License-Identifier: MIT-0
---
- name: Suppression plusieurs utilisateurs  
  become: true
  ansible.builtin.user:
    name: "{{ item.name }}"
    state: absent
    remove: yes
  loop: "{{ users }}"

